project: SWG
lead_project: Open Source Security
label:
upstream: "Yes"
acceptance_criteria:
time_estimate: 4
issuetype: Epic
summary: PKCS#11 - Session authentication
epic_name: PKCS#11 - Session authentication
description:
        - |
          h1. Deliverables
          This Epic is about implementing the session authentication API. The list of PKCS#11 functions that we intend to implement in this Epic are:
          * C_InitPIN(), initializes the normal user’s PIN
          * C_SetPIN(), modifies the PIN of the current user
          * C_Login(), logs into a token
          * C_Logout(), logs out from a token

          For most functions mentioned here, we need to write code in optee_os, optee_client and optee_test. I.e., these functions are called on the client side, but in most cases there is a 1:1 mapping to a function on secure side also (as a service in a Trusted Application).

          h1. Out of Scope
          N/A

          h1. Risks and Assumptions
          Assumptions: possible to implement this as a standalone component.

          h1. Acceptance Criteria
          |
          ||Criteria||Status||Closeout Notes/Links||
          |Patches upstream implementing PKCS#11 session authentication support in OP-TEE (Armv7-A and Armv8-A)|(x)|Patches can be found [here (FIXME update link) |https://github.com/XYZ]|
          |Patches upstream enabling tests for the PKCS#11 session authentication |(x)|Patches can be found [here (FIXME update link) |https://github.com/XYZ]|
          | C_InitPIN() implemented/upstream |(x)| |
          | C_SetPIN() implemented/upstream |(x)| |
          | C_Login() implemented/upstream |(x)| |
          | C_Logout() implemented/upstream |(x)| |

          Legend:
          ||(/) Done, (x) Not Done, (!) Doesn't apply (note the reason)||
